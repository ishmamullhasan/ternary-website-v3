#!/usr/bin/env sh

# Run eslint --fix + prettier on staged files (see .lintstagedrc.json). Lint is now
# blocking: an eslint *error* on a staged file fails the commit. lint-staged stashes,
# fixes, and re-stages automatically.
pnpm exec lint-staged
